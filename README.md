# RAG-System
Production-style Retrieval-Augmented Generation (RAG) system that answers questions from custom documents with grounded responses and inline citations using embeddings, vector search, and LLMs.
